#include<stdio.h>

int main()
{
	 int x;
	 int a;
	 short int b;
	 long int c;
	 float d;
	 char ch;
	 printf("la taille de la variable x est : %d\n", sizeof(x));
	 printf("la taille de la variable a est : %d\n", sizeof(a));
	 printf("la taille de la variable b est : %d\n", sizeof(b));
	 printf("la taille de la variable c est : %d\n", sizeof(c));
	 printf("la taille de la variable d est : %d\n", sizeof(d));
	 printf("la taille de la variable ch est : %d\n", sizeof(ch));

	return 0;
}
